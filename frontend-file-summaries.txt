{
  "frontend/src/App.tsx": "Root app component using `isAdmin` from `useAuth()` (no longer uses removed `session` property) to control page routing and titles within the authenticated dashboard layout.",
  "frontend/src/pages/HistoryPage.tsx": "Admin-only history search page with account number and date range filters; fixed conflicting `text-foreground`/`text-muted-foreground` classnames and replaced `flex-shrink-0` with `shrink-0` throughout.",
  "frontend/src/pages/PassbookPage.tsx": "Passbook print page updated to use `userAccountNumber` from the updated `useAuth()` hook (replacing the removed `session` property) for user account lookup and access restriction.",
  "frontend/src/components/BankDetailForm.tsx": "Modal form for adding/editing bank detail records with 4 fields, validation, toast success/error notifications, loading spinner, and closes only on successful save (stays open on error for retry).",
  "frontend/src/pages/BankDetailsPage.tsx": "Bank details management page updated to use `onOpenChange` prop (instead of `onClose`) when rendering `BankDetailForm`, with a local `handleOpenChange` handler that also clears the edit state.",
  "frontend/src/pages/LoginPage.tsx": "Login page with admin/user tab toggle; properly awaits II login for admin and shows friendly error messages for cancellation or failure; includes loading spinner and toast notifications.",
  "frontend/src/components/Sidebar.tsx": "Collapsible sidebar navigation using `userAccountNumber` and `isAdmin` from the updated `useAuth()` hook (replacing the removed `session` property) for user info display and role-based menu filtering.",
  "frontend/src/components/DashboardLayout.tsx": "Main dashboard shell with sidebar, top bar, and page content area; replaced `flex-shrink-0` with `shrink-0` on the header element.",
  "frontend/src/components/StudentForm.tsx": "Modal form for adding/editing student records with 7 fields, validation, toast success/error notifications, loading spinner on submit button, and closes only on successful save (stays open on error for retry).",
  "frontend/src/components/AccountForm.tsx": "Modal form for adding/editing bank accounts with student and IFSC dropdowns, auto-fill bank name, toast success/error notifications, loading spinner, and closes only on successful save.",
  "frontend/src/pages/StudentPage.tsx": "Admin-only student management page that converts `bigint` id to `number` before calling `deleteStudent.mutateAsync`, and uses `onOpenChange` prop (instead of `onClose`) when rendering `StudentForm`.",
  "frontend/src/pages/AccountPage.tsx": "Admin-only account management page updated to use `onOpenChange` prop (instead of `onClose`) when rendering `AccountForm`, with a local `handleOpenChange` handler that also clears the edit state.",
  "frontend/src/pages/HomePage.tsx": "Admin-only dashboard home page with 5 colorful stat cards and recent transactions list; replaced `flex-shrink-0` with `shrink-0`.",
  "frontend/src/hooks/useAuth.ts": "Auth context provider using `createElement` (no JSX) to avoid parse errors in a `.ts` file; properly awaits Internet Identity login for admin (errors propagate so failed/cancelled II login does not grant admin session), and handles user login by storing account number in localStorage.",
  "frontend/src/hooks/useQueries.ts": "Complete React Query hooks for all backend operations (students, accounts, transactions, bank details, user profile) with actor readiness checks in every mutation (throws if actor is null), proper cache invalidation on success, and error logging.",
  "frontend/src/pages/TransactionPage.tsx": "Transaction management page with an expanded form that includes Date (defaulting to today, editable), Previous Balance (read-only, auto-computed from selected account's existing transactions), and Total Amount (read-only, real-time computed as previousBalance ± amount based on transaction type); the transactions table also displays the mागील शिल्लक and एकूण रक्कम columns from the backend-returned data."
}